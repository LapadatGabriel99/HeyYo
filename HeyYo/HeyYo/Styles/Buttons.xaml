<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d"
                    >
    <!-- Regular Button -->
    <Style x:Key ="RegularButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseStyleButton}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXLarge}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BackgroundColor" Value="{StaticResource HeyYoMainDarkBlue}"/>
        <Setter Property="TextColor" Value="{StaticResource HeyYoMainWhite}"/>
        <Setter Property="Margin" Value="0, 10"/>
    </Style>
    
    <!-- Form Input Button -->
    <Style x:Key="FormInputButton" TargetType="{x:Type Button}" BasedOn="{StaticResource RegularButton}">
        
    </Style>
</ResourceDictionary>